{% macro section(title, class, is_dark=false) %}
  <section class="nes-container with-title {{ class }} {{ 'is-dark' if is_dark }}"
           aria-labelledby="{{ class }}-title">
    <h2 id="{{ class }}-title" class="title">{{ title }}</h2>
    <div class="content">
      {{ caller() }}
    </div>
  </section>
{% endmacro %}

{% macro button(title, class="", type="is-primary") %}
  {# is-primary #}
  {# is-success #}
  {# is-warning #}
  {# is-error #}
  {# is-disabled #}
  <button class="nes-btn {{ type }} {{ class }}" type="button">
    <span>{{ title }}</span>
  </button>
{% endmacro %}

{% macro list(is_circle=true) %}
  <ul class="nes-list {{ 'is-circle' if is_circle else 'is-disc' }}">
    {{ caller() }}
  </ul>
{% endmacro %}